package com.CampusToursLive.domain.tour;

/** Matches the PostgreSQL enum type {@code tour_topic} and the OpenAPI TourTopic. */
public enum TourTopic {
    GENERAL_CAMPUS,
    DORM_HOUSING,
    DINING_STUDENT_LIFE,
    MAJOR_SPECIFIC,
    INTERNATIONAL_STUDENT,
    PARENT_FOCUSED,
    FRESHMAN,
    TRANSFER
}
